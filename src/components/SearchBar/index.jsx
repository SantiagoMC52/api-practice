import React from 'react';

const SearchBar = () => (
  <div>
    <input
      placeholder="Search..."
      required
    />
    <button type="button">Search</button>
  </div>
);

export default SearchBar;
